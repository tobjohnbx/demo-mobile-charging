# Zeitzone
TZ=Europe/Berlin

# Benutzer-Verzeichnis (falls nicht gesetzt, wird $HOME verwendet)
USERDIR=${HOME}

# Home Assistant Version (optional, falls Sie eine spezifische Version möchten)
# HA_VERSION=stable
# MATTER_VERSION=stable

# Home Assistant HTTP Port (Standard: 8123)
HA_PORT=8123

# Matter Server WebSocket Port (Standard: 5580)
MATTER_PORT=5580

# Bluetooth Adapter (0 = erster Adapter)
BLUETOOTH_ADAPTER=0

# Log Level für Matter Server (debug, info, warning, error)
MATTER_LOG_LEVEL=info